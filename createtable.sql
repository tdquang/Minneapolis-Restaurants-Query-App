DROP TABLE IF EXISTS MinneapolisRestaurant;
CREATE TABLE MinneapolisRestaurant (
  name text,
  address text,
  postcode numeric,
  tel text,
  neighborhood text,
  website text,
  cuisine text,
  price numeric,
  rating float,
  deliver text,
  takeout text
);

